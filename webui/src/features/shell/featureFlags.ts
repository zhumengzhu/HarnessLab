/** Web UI feature toggles — flip when a surface is ready for daily use. */

/** Improvement proposals review panel (`harnesslab propose` output). CLI/API stay available. */
export const SHOW_PROPOSALS_UI = false;
